{
    "build":"cd client && npm install && npm run build",
    "outputDirectory":"client/dist",
    "frameworks":"vite",
    "rewrites":[
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}